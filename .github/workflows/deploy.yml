name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Build All
        run: |
          # Build Admin
          cd "Admin Portal"
          npm install
          npm run build
          cd ..
          
          # Build Student
          cd "Student portal"
          npm install
          npm run build
          cd ..
          
          # Build Teacher
          cd "Teacher Portal"
          npm install
          npm run build
          cd ..
          
          # Assemble Public
          mkdir -p public
          cp -r "Landing Page"/* public/
          mkdir -p public/admin public/student public/teacher
          cp -r "Admin Portal"/dist/* public/admin/
          cp -r "Student portal"/dist/* public/student/
          cp -r "Teacher Portal"/dist/* public/teacher/

      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
