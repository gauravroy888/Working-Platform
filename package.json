{
  "name": "edtech-island-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build:teacher": "npm run build --prefix \"Teacher Portal\"",
    "build:admin": "npm run build --prefix \"CODE Message test/Admin Portal\"",
    "build:student": "npm run build --prefix \"CODE Message test/Student portal\"",
    "build:all": "npm run build:teacher && npm run build:admin && npm run build:student"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1110.0"
  }
}
